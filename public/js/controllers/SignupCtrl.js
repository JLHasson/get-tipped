angular.module('SignupCtrl', [])
    .controller('SignupController', ['$scope', '$window', '$rootScope', '$auth', function($scope, $window, $rootScope, $auth) {

        // $scope.isAuthenticated = function() {
        //     //Check if logged in
        //     return $auth.isAuthenticated();
        // };

}]);
